{
  "name": "ux-design-system",
  "type": "module",
  "version": "0.1.0",
  "description": "Le design system de Judith Heckmann : 115 tokens exportés de Figma et les composants qui en sont faits.",
  "homepage": "https://equipollente.github.io/UX-design-system/",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Equipollente/UX-design-system.git"
  },
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.12.0"
  },
  "// exports": "Ce que les sites consommateurs ont le droit d'importer. Rien d'autre n'est atteignable : la surface publique du système est décidée ici, pas subie.",
  "exports": {
    "./components/*": "./src/design-system/components/*",
    "./layouts/*": "./src/design-system/layouts/*",
    "./lib/*": "./src/design-system/lib/*",
    "./styles/*": "./src/design-system/styles/*",
    "./data/tokens.json": "./src/design-system/data/tokens.json",
    "./package.json": "./package.json"
  },
  "// files": "Ce qui part dans le paquet. Les icônes en font partie : inlinées, elles sont du code et non des assets, et un site consommateur les reçoit avec le système. public/ reste exclu — les images, elles, sont le choix du site qui les monte, et le Nav reçoit ses chemins en props.",
  "files": [
    "src/design-system"
  ],
  "peerDependencies": {
    "astro": "^7.0.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run tokens && npm run icons && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "astro": "astro",
    "tokens": "node scripts/build-tokens.mjs",
    "icons": "node scripts/build-icons.mjs"
  },
  "// devDependencies": "Astro est en devDependencies, pas en dependencies : un site qui installe le système ne doit pas se retrouver avec un second Astro dans son node_modules. La version attendue est déclarée en peerDependencies.",
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "astro": "^7.2.1",
    "typescript": "^6.0.3"
  }
}
